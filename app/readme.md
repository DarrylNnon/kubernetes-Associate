# process

cd app/
  ```sh
   32  docker login
   33  bundle install
   34  bundle exec server.rb 
   35  bundle server.rb 
   36  ruby exec server.rb 
   37  ruby server.rb 
   38  docker login
   39  $ docker build --tag hello .
   40  docker build --tag hello .
   41  ruby --version
   42  rvm list
   43  bundle exec ruby server.rb
   44  docker build . -t sinatra-sample
   45  ruby --version
   46  docker build . -t sinatra-sample
   47  docker images
   48  history
```